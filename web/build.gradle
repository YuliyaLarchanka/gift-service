mainClassName = 'com.epam.esm.web.Main'

shadowJar {
    mergeServiceFiles()
}

dependencies {
    implementation project(':service')
    implementation project(':repository')
}

jar {
    manifest {
        attributes 'Multi-Release': 'true'
    }
}